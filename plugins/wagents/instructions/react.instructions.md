# React Instructions

- Use functional components with hooks; no new class components.
- Keep components small and focused; co-locate styles and tests.
- Validate props at boundaries; handle loading, empty, and error states.
- Preserve accessibility: labels, focus management, keyboard navigation, contrast.
- Avoid unnecessary dependencies; inspect stack before adding UI libraries.
