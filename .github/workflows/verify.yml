name: verify-release
on:
  push:
  pull_request:
jobs:
  verify:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Release gate
        shell: pwsh
        run: ./scripts/verify-release.ps1
