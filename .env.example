# wagents — local env (never commit)
# Copy to .env for local use only. .env is gitignored.

# Tier 1
GITHUB_TOKEN=

# Tier 2
TAVILY_API_KEY=
SENTRY_AUTH_TOKEN=
SONAR_TOKEN=
SONAR_HOST_URL=
SEMGREP_APP_TOKEN=

# Conditional (disabled by default)
POSTGRES_CONNECTION_STRING=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Pinned refs (set on first vendor, see docs/skills.md)
CLAUDE_RED_REF=
